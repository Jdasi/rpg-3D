
public enum PlayerId
{
    Invalid = 0,

    One, // Server
    Two,
    Three,
    Four,
}

public enum EntityId
{
    Invalid = 0,
}

public enum CharacterId
{
    Invalid = 0,
}

public enum TeamId
{
    Invalid = 0,

    Player,
    Enemy,
}

public enum TickTiming
{
    EndOfTurn,
    StartOfTurn,
}
